namespace EzCodeProblems
{
    // ideal: problems can be downloaded as a text file.
    //   funtion is just declared with comments. We can find it either with attribute or just only func in the code?
    //   verify... dsl?
    public class Problem0002
    {
        // Return the sum of a and b.
        public int Sum(int a, int b)
        {
            return a + b;
        }


    }
}
